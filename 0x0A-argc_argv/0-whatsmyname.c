#include <stdio.h>
/**
 * main - test argv indexed zero
 * @argc: count
 * @argv: array of arguments
 * Return: 0
 */
int main(int __attribute__ ((unused)) argc, char *argv[])
{
	printf("%s\n", argv[0]);
	return (0);
}
