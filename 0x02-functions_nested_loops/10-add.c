#include "main.h"
/**
 * add - function that adds two integers and returns the result
 * @a: First operand
 * @b: Second operand
 * Return: result
 */
int add(int a, int b)
{
	int c = a + b;

	return (c);
}
