#include "main.h"
#include <stdio.h>
#include <stdlib.h>
#include <ctype.h>
#include <string.h>
/**
 * _strlen - _strlen
 * @s: string
 * Return: lenght of string
 */
int _strlen(char *s)
{
	return (strlen(s));
}
