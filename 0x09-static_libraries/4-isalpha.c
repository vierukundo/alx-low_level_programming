#include "main.h"
#include <stdio.h>
#include <stdlib.h>
#include <ctype.h>
#include <string.h>
/**
 * _isalpha - _isalpha
 * @c: character
 * Return: 1 or 0
 */
int _isalpha(int c)
{
	return (isalpha(c));
}
