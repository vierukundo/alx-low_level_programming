#include "main.h"
#include <stdio.h>
#include <stdlib.h>
#include <ctype.h>
#include <string.h>
/**
 * _strcmp - compare strings
 * @s1: string one
 * @s2: string two
 * Return: 0 if they are the same. 1 if s1 > s2. -1 if s2 > s1
 */
int _strcmp(char *s1, char *s2)
{
	return (strcmp(s1, s2));
}
